[package]
name = "helu-common"
version = "0.1.0"
description = "Linux Helu shared types and D-Bus interface definitions"
edition = "2021"

[dependencies]
zbus = { version = "5.14.0" }
serde = { version = "1.0", features = ["derive"] }
