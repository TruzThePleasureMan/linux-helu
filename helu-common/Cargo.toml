[package]
name = "helu-common"
version = "0.1.0"
description = "Shared definitions and D-Bus interfaces utilized across the workspace"
edition = "2021"

[dependencies]
zbus = { version = "5.14.0" }
serde = { version = "1.0", features = ["derive"] }
